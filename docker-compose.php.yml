version: '3'
services:
  php:
    build: ./docker/php
    image: algorithms_php
    container_name: algs-php
    user: ${UID}:${UID}
    volumes:
      - .:/var/www
      - /tmp:/tmp
      # Composer cache.
#      - ~/.cache/composer:/.composer/cache
    working_dir: /var/www
